/** Browser-facing re-export of the canonical edge-safe formatters. */
export {
  formatAcronymLabel,
  formatCaseLabel,
  formatDeliverableLabel,
  formatFigureLabel,
  formatMilestoneLabel,
  formatParticipantLabel,
  formatTableLabel,
  formatTaskLabel,
  formatWPLabel,
  formatWPChipLabel,
  wpShowShortName,
} from '../../supabase/functions/_shared/referenceLabels';
